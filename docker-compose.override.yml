services:
  api:
    command: uv run uvicorn src.main:app --host 0.0.0.0 --port 8000 --reload
    environment:
      - DEBUG=true
    volumes:
      - ./backend/src:/app/src
      - ./backend/alembic:/app/alembic
